import styled from "@emotion/styled";
import { colorTheme } from "../color/colorTheme";

export const ChipTypography = styled("div")({
  color: colorTheme.blackText,
  fontSize: "calc(12px + .15vw)",
});
