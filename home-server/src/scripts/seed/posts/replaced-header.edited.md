This post was published with a placeholder header image and then edited. The edit uploaded a new header and rewrote this text, so the post now has two revisions.

The header above should be the green one named `final-header.jpg`. The placeholder, `placeholder-header.jpg`, belongs to the first revision and should no longer be served.
