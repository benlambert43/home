An image is uploaded once and can be used as often as the text likes. Each use can have its own description and its own title.

![The divider, the first time](./images/divider.png "Hover for a title: first")

A paragraph between the first and second use.

![The divider, the second time](./images/divider.png "Hover for a title: second")

A paragraph between the second and third use.

![The divider, the third time](./images/divider.png)

The post also carries an image the text never uses, which is allowed: it is stored with the post and simply never shown.
